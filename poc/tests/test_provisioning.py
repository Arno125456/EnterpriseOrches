"""
ProvisioningState — admit sequences, headroom arithmetic, budget rejection, snapshot/restore round-trip.

Spec: docs/System_Architecture_v2.md §6.6.
Covers build step 5. Owner: 083
"""
import pytest

pytestmark = pytest.mark.skip(reason="build step 5 not yet implemented")

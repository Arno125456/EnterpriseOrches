pulp>=2.8            # Free, open-source MILP modeling + CBC solver (offline baseline)
ortools>=9.9          # Optional: CP-SAT for time-limited near-optimal comparison
pytest>=8.0            # Test framework
